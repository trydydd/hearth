[hearth]
# Replace with the IP address or hostname of your Raspberry Pi.
# Example:
#   hearth ansible_host=192.168.1.100 ansible_user=pi
#
# Run with:
#   ansible-playbook -i inventory/production site.yml
